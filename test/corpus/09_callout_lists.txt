================================================================================
Simple callout list
================================================================================

<1> First callout
<2> Second callout
<10> Tenth callout

--------------------------------------------------------------------------------

(source_file
  (callout_list
    (callout_item
      (CALLOUT_MARKER)
      (inline_seq_nonempty
        (plain_text)))
    (callout_item
      (CALLOUT_MARKER)
      (inline_seq_nonempty
        (plain_text)))
    (callout_item
      (CALLOUT_MARKER)
      (inline_seq_nonempty
        (plain_text)))))

================================================================================
Not a callout list - missing space after marker
================================================================================

<1>NotACallout

<2>NotACallout

--------------------------------------------------------------------------------

(source_file
  (paragraph
    (inline_seq_nonempty
      (formatting_fallback)
      (plain_text)
      (formatting_fallback)
      (plain_text)))
  (paragraph
    (inline_seq_nonempty
      (formatting_fallback)
      (plain_text)
      (formatting_fallback)
      (plain_text))))
