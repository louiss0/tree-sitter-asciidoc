# TODO

## Tests that are failing
- Not run in this session.

## What bugs are present
- None confirmed; verify checklist marker parsing and document header parsing in `npx tree-sitter test`.

## What to do next
- Confirm highlight queries still match new fields/nodes for list checkboxes and macro titles.
- Run `npx tree-sitter parse asciidoc-syntax-reference.adoc`. look for errors! 
 - After finding the errors find ways to solve the problem! 
- Run  `npx tree-sitter parse` on the files in the examples directory make a summary of the errors. If found.
